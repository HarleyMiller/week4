const config = {
  host : "localhost",
  port : 8889,
  user : "root",
  password : "root", //blank for windows
  database : "cooperStuff"
};

module.exports = config; //makes this a public file that node can read
